source "https://rubygems.org"

# Optional: only needed if you want to preview the site locally with
# `bundle exec jekyll serve`. GitHub Pages does not use this file.
gem "github-pages", group: :jekyll_plugins
